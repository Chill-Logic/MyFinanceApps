<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.DayNight.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="android:editTextBackground">@drawable/rn_edit_text_material</item>
        <!-- The app manages its own dark theme in JS (ThemeProvider) and never opted into Android's
             DayNight resource system (no values-night folder) — without this, Android's automatic
             Force Dark contrast heuristic adds unwanted highlight boxes behind text on API 29+. -->
        <item name="android:forceDarkAllowed">false</item>
    </style>

</resources>
